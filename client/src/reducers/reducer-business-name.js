const businessName = (state = null, action) => {
  // switch (action.type) {
  //   case 'GET_BUSINESS_NAME':
  //     return /* add the way to somehow get business name here */;
  //   default:
  //     return state;
  // }
  return 'Hack Reactor';
};

export default businessName;
